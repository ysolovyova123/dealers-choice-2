{/* <section>
          <h3>Tables (Select One)</h3>
            <ul id='tableList'>
              {
                tables.map(table => {
                  return (
                  <li key = {table.id} className={ table.id }>
                    <a onClick = {() => this.displayGuests()}>
                      {table.table}
                    </a>
                  </li>
                  )
                })
              }
            </ul>
          </section> */}
