{/* <section>
          <h3>Guests At The Table</h3>
            <ul id='guestList'>
              {
                guests.map(guest => {
                  return (
                  <li key = {guest.id} className = {guest.id }>
                    {guest.name}
                  </li>
                  )
                })
              }
            </ul>
          </section> */}
